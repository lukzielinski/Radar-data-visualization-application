// import api from './api/api';
// import { logIn, logOut } from './stores/Login';

// export async function checkIsUserLogged (): Promise<boolean> {
//   const user = (await api.login.getCurrentUser()).data;
  
//   if (user) {
//     logIn(user)
//     return true;
//   } else {
//     logOut()
//     return false;
//   }
// }